import { Component } from "react";

class ClassBasedComponentProps extends Component {
  render() {
    // console.log(this.props);
    return (
      <>
        {/* <p>
          My name is {this.props.name} and my age is {this.props.age}
        </p>
        <div>
          {this.props.arr.map((value) => {
            return <p>{value}</p>;
          })}
        </div>
        <section>{this.props.demo()}</section> */}
      </>
    );
  }
}
export default ClassBasedComponentProps;
